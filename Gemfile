source 'https://rubygems.org'

gem 'httparty', '~> 0.18'  # HTTP client library
gem 'dotenv', '~> 2.7'  # Environment variables management

# Add more gems if needed

group :development, :test do
  gem 'rubocop', '~> 1.0'  # Code linter
  gem 'pry', '~> 0.14'  # Interactive console for debugging
end

gem "mocha", "= 2.0.2"
